#include "LongNumber.h"
#include <vector>

void Increment(MyString& str)
{
	/*
	std::vector<char&> vec;
	vec.reserve(str.length);
	int offset = 1;
	while (str.symbols[str.length - offset] == '9')
	{
		vec.push_back(str.symbols[str.length - offset]);
		offset++;
	}

	if (offset == str.length + 1) // строка из девяток
	{
		str = MyString("1") + MyString('0', str.length);
		str.length++;
	}

	else
	{
		for (auto& symbol : vec) symbol = '0';
		str.symbols[str.length - offset]++;
	}
	*/
}
